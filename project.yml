name: LogicAudioAssistant
options:
  bundleIdPrefix: com.marcboyer.logicaudioassistant
  deploymentTarget:
    macOS: "14.0"
packages:
  CorePackage:
    path: .
targets:
  CompanionMacApp:
    type: application
    platform: macOS
    sources: [apps/CompanionMacApp]
    info:
      path: apps/CompanionMacApp/Info.plist
      properties:
        CFBundleDisplayName: Logic Audio Assistant
        CFBundleURLTypes:
          - CFBundleURLName: com.marcboyer.logicaudioassistant.tutor
            CFBundleURLSchemes:
              - tracksmith
        LSApplicationCategoryType: public.app-category.music
        NSHighResolutionCapable: true
    entitlements:
      path: apps/CompanionMacApp/CompanionMacApp.entitlements
      properties:
        com.apple.security.application-groups:
          - KDV9RC892F.com.marcboyer.logicaudioassistant
        com.apple.security.network.client: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.marcboyer.logicaudioassistant
        PRODUCT_NAME: Logic Audio Assistant
        SWIFT_VERSION: 6.0
    dependencies:
      - package: CorePackage
        product: SharedIPC
      - package: CorePackage
        product: SessionCore
      - package: CorePackage
        product: PreviewWorkflow
      - package: CorePackage
        product: PreviewAudition
      - package: CorePackage
        product: DSPCore
      - package: CorePackage
        product: PlanSchema
      - package: CorePackage
        product: AgentCore
      - package: CorePackage
        product: AudioAnalysis
      - package: CorePackage
        product: ProductionIntelligence
      - package: CorePackage
        product: ProductionTutor
      - package: CorePackage
        product: TutorConversation
      - package: CorePackage
        product: TutorLogicObserver
      - package: CorePackage
        product: VocalProduction
      - target: AssistantAudioUnitExtension
        embed: true
  AssistantAudioUnitExtension:
    type: app-extension
    platform: macOS
    sources: [plugins/AudioUnit/AudioUnitExtension]
    entitlements:
      path: plugins/AudioUnit/AudioUnitExtension/AssistantAudioUnitExtension.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.application-groups:
          - KDV9RC892F.com.marcboyer.logicaudioassistant
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.marcboyer.logicaudioassistant.AudioUnit
        PRODUCT_NAME: Logic Audio Assistant AU
        SWIFT_VERSION: 6.0
        APPLICATION_EXTENSION_API_ONLY: YES
    info:
      path: plugins/AudioUnit/AudioUnitExtension/Info.plist
      properties:
        CFBundleDisplayName: Logic Audio Assistant AU
        NSExtension:
          NSExtensionPointIdentifier: com.apple.AudioUnit-UI
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).AudioUnitViewController
          NSExtensionAttributes:
            AudioComponents:
              - name: "Marc Boyer: Logic Audio Assistant"
                description: Conversational deterministic audio processing
                factoryFunction: $(PRODUCT_MODULE_NAME).AudioUnitViewController
                manufacturer: ExAI
                type: aufx
                subtype: LgAA
                version: 65536
                sandboxSafe: true
                tags:
                  - Effects
    dependencies:
      - package: CorePackage
        product: CAtomics
      - package: CorePackage
        product: AudioAnalysis
      - package: CorePackage
        product: DSPCore
      - package: CorePackage
        product: PlanSchema
      - package: CorePackage
        product: SharedIPC
