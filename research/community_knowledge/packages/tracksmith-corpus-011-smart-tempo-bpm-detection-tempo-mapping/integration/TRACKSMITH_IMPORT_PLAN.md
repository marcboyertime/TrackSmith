# TrackSmith Import Plan

Import the development package as a namespaced, immutable package record. Generate only the bounded canonical runtime projection. Merge no candidate procedure, navigation sequence, scenario, evaluation, SQLite database, test alias, or expected-answer metadata into live Tutor context. Rebuild the unified retrieval index additively and record the package version, hashes, source IDs, review states, collision mappings, and runtime output hash.
