# Tool Contract

The proposed Tutor retrieval tool is read-only and returns bounded canonical timing guidance, clarification policy, competing hypotheses, tradeoffs, source references, status fields, and diagnostic confidence. It never returns candidate procedure steps, menu paths, click sequences, evaluation expectations, scenarios, or database paths. It never edits Logic.
