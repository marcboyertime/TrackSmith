# Package Sequence

Package 010 depends additively on Packages 001–009. It must not overwrite prior IDs, migrations, indexes, source records, evaluations, or review states. The importer produces a separate runtime projection and preserves the complete package as development/review material.
