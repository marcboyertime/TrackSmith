# Validation Report

Run `python3 tools/validate_package.py --full` after extraction. The validator checks archive/checksum integrity, schemas, sources, status preservation, exact retrieval uniqueness and FTS top-1, diagnostic fixture classification, additive compatibility, prerequisites, SQLite integrity, and runtime projection purity.
