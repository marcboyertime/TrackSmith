# Start Here for Codex

This is TrackSmith Corpus Package 006: Saturation / Harmonic Distortion + Transient Shaping.

1. Read `package_manifest.json` and `integration/PACKAGE_SEQUENCE.md`.
2. Run `python3 tools/validate_package.py`.
3. Inspect representative retrieval with `tools/query_corpus.py`.
4. Run the importer in dry-run mode before modifying TrackSmith.
5. Integrate through the scalable community/candidate retrieval layer established for prior packages. Do **not** compile this corpus into `GeneralTutorKnowledge.generated.swift`.
6. Preserve every review state, source ID, package namespace, contradiction, myth, and Logic verification boundary.
7. Do not give the Tutor mutation authority. Exact Logic operations remain user-performed and version-scoped.
8. Use level-matched, one-variable listening experiments. Keep nonlinear tone, envelope change, loudness, measurement, provider listening, and user confirmation distinct.
