# Start here for Codex

This is Package 009. Integrate only after Packages 001–008 are stable.

1. Run `python3 tools/validate_package.py`.
2. Run `python3 tools/inspect_package.py`.
3. Query representative cases with `python3 tools/query_corpus.py "analog modeled headroom"`.
4. Run `python3 tools/import_to_tracksmith.py --target /path/to/TrackSmith --dry-run`.
5. Integrate through the existing scalable community/retrieval layer; do not compile this corpus into `GeneralTutorKnowledge.generated.swift`.
6. Preserve candidate review states and all read-only Tutor authority boundaries.

The Tutor must distinguish signal level from monitor level, control from audio routing, peak from loudness, sample peak from true peak, and specifications from artistic targets.
