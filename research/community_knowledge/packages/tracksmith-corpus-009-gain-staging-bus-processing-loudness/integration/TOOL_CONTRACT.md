# Tool contract

Expose this package only through bounded read-only retrieval. Return compact relevant Q&A, distinctions, one experiment, tradeoffs, disagreement, source IDs, package version, and candidate review state. Never return arbitrary files, raw web pages, shell commands, or Logic mutation authority. Exact Logic procedures remain separate, version-scoped candidates.
