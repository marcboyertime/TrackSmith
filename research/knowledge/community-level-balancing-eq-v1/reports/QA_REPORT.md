# QA report

- Canonical IDs are unique.
- Source references resolve.
- Utterance and scenario links resolve.
- All knowledge records retain candidate review status.
- Level balancing and equalization remain separate domains.
- SQLite FTS5 returns representative level and EQ queries.
- Raw forum bodies are absent.
- Logic procedure candidates do not receive execution authority.
- Package sequence explicitly requires package 1 first.

Counts are recorded in `reports/stats.json` and validated by `tools/validate_package.py`.
