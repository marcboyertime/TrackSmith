# Package sequence

1. Integrate and validate the earlier TrackSmith Vocal + Quantization corpus package.
2. Confirm its migrations, retrieval tool, evidence classes, and tests are stable.
3. Then integrate this **Level Balancing + EQ** package as a separate corpus version/domain expansion.
4. Do not overwrite earlier canonical IDs, source IDs, migrations, or evaluation fixtures.
5. Prefer a single scalable CommunityKnowledge/Corpus retrieval layer with versioned domain manifests rather than compiling either package into generated Swift.
