This is TrackSmith corpus Package 3: Compression + Arrangement + Frequency Allocation.

Integrate it only after Package 1 and Package 2 are stable. Extract it outside the
repository, run `python3 tools/validate_package.py`, then begin with
`integration/START_HERE_FOR_CODEX.md`. Preserve all candidate review states and all
existing Tutor authority boundaries. Do not compile the entire corpus into generated
Swift; use the scalable retrieval layer established for the earlier packages.
