This is TrackSmith corpus Package 4: Reverb + Delay.

Integrate it only after Packages 1–3 are stable. Extract it outside the TrackSmith
repository, run `python3 tools/validate_package.py`, and begin with
`integration/START_HERE_FOR_CODEX.md`.

Preserve every candidate review state and all earlier package IDs and migrations.
Use the scalable retrieval/database layer; do not compile this corpus into generated
Swift. Keep reviewed knowledge, community synthesis, model listening, measurements,
and user-confirmed outcomes distinct. Do not add Logic mutation authority.
