# Logic integration capability matrix

Status reconciled 2026-08-08 against the frozen pre-Vocal baseline at `406b446`.
Local evidence: macOS 26.3 arm64, Logic Pro 12.3
(`com.apple.logic10`, universal binary), Swift 6.3.3, Xcode 26.6 / SDK 26.5.
“Documented” means a public vendor document exists; “verified” means this repository
ran a proof. Nothing in this table claims Logic-host success unless stated.

Apple documents AUv3 effects, buses, render blocks, parameters, transport/musical
callbacks, persistence, bypass and latency in [`AUAudioUnit`](https://developer.apple.com/documentation/audiotoolbox/auaudiounit), and Logic documents use of [AUv3 extensions](https://support.apple.com/guide/logicpro/work-with-audio-units-in-logic-pro-for-mac-lgcp22a0dab0/mac).

| Mechanism | Read / write capability | Selection / audio / plug-ins / automation | Apple Silicon, sandbox, permission | Evidence and production judgment |
|---|---|---|---|---|
| AUv3 effect | Reads its input buses and host callbacks; writes its output and own state/parameters | No selected-region API; input-stream audio only; controls own parameters; host may automate them | Supported on Apple Silicon; app and extension are sandboxed; no microphone permission for insert audio | The installed Apple-development-signed app/extension share Team ID `KDV9RC892F`, strict-verify, are registered, and pass `auval` out of process through 192 kHz. The current-source production host verifies capture lifetime, exact graph execution/revision, lifecycle-locked snapshot/format/CAS/lock/state commit, automation contracts, global bypass, two-instance isolation and `fullState` restore. Historical Logic 11.2.2 proved the 44.1 kHz stereo workflow; the separate Logic 12.3 lane proved the 44.1 kHz mono workflow, save/reload and two-live-instance targeted isolation with exact installed-binary fingerprints. **Production core for the exercised workflows.** |
| TrackSmith deterministic DSP | Executes only validator-accepted TrackSmith graphs: gain/polarity/filter/EQ, compression, de-essing, expander/gate, saturation/soft clipping, stereo width, fixed-time feedback delay, bounded algorithmic room, and sample-peak limiting | Own graph only; no Logic-native insertion, parameter, automation, project, or file authority | Offline renderer and AU use the same deterministic sample loops; AU storage is compiled/preallocated and source files remain unchanged | Expander, delay, and reverb require explicit algorithm version 1 and have node, time, feedback, decay, range, rate, and channel bounds. Unit evidence covers exact delay repeats/crossfeed, expander link/hold/hysteresis/range, reverb tail/decay/rates, reset, serialization, finite-input recovery, disabled bypass, and offline/borrowed-buffer parity. The expanded custom-host Release graph passed at 16.9 us mean/18.2 us p99 for 128 frames at 48 kHz; the heap interposer observed zero operations across 4,000 callbacks. These facts establish bounded implementation behavior, not artistic superiority or equivalence to a Logic processor. **G3 implementation candidate; release/sanitizer/installed-host and perceptual gates remain open.** |
| AUv2 | Similar audio/parameter host contract; component bundle distribution | Does not add a Logic project API | Apple says AUv2 is maintenance mode; sandbox compatibility varies | [Apple recommends AUv3 for new work](https://developer.apple.com/documentation/audiotoolbox/hosting-audio-unit-extensions-using-the-auv2-api). **Fallback only if a measured Logic blocker requires it.** |
| Native companion | Owns UI, local analysis, preview rendering/cache, synchronized audition, session selection, provider networking, Keychain credentials, and durable conversation state | Cannot introspect Logic merely by being installed | Sandboxed; signed App Group enabled; cloud text/measurement reasoning requires an explicit selected provider, Keychain item, and consent; raw audio is not uploaded | The SwiftUI app discovers AU heartbeats, requests recent capture, shows a waveform, renders and auditions three bounded-loudness-match options, exposes editable/lockable cards, performs typed revision/undo/redo, and sends capture-bound exact commit, bypass and revert commands. Its compact rationale panel visibly distinguishes validated intent, hypothesis/risk, measured evidence/confidence, configured versus provider-reported model identity, and completed gates without exposing hidden reasoning. The deterministic exchange passed in Logic 11.2.2 and 12.3. OpenAI/Gemini adapters have live six-case cross-provider evidence plus Gemini 30-case cloud evidence. A direct Gemini/Logic 12.3 session proved free-form interpretation, preview/reference revision, lock preservation, exact post-render commit, bypass/restore, save/reload, provider-offline graph recovery, unchanged source bytes and instance isolation. **Production deterministic/provider foundation for the exercised workflow.** |
| Source-aware production interpretation | Reads free-form user text, explicit source class, typed measurements, relevant knowledge, state and references; writes validated intent, hypotheses and bounded plan candidates | No extra Logic authority; source class is explicit rather than inferred from a project DOM | Deterministic offline fallback plus companion-only provider-neutral cloud adapters; no raw-audio upload | Six source classes, all 28 requested provenance-tagged production descriptors plus four explicit preservation concepts, a separate 14-entry source-scoped abstract-language advisory catalog, eight required flows, 420 semantic/adversarial cases, strict model validation, durable typed references, 30/30 generated-audio pipeline cases, live OpenAI/Gemini provider evidence, and direct Logic frontier evidence are implemented. The abstract catalog preserves alternate meanings, contradictions, non-DSP causes and clarification policy but contains no node/parameter/host/measurement authority. Evidence, uncertainty, preservation, risk and listening-decisive flags stay explicit; every graph remains TrackSmith-generated and validator-gated. **Production Intelligence v1 foundation proven for the declared scope; semantics and artistic preference remain fallible and listening-decisive.** |
| Logic-native tool knowledge | Retrieves bounded advisory facts about documented Logic 12.3 effects, instruments, and explicitly named editor tools; workflow/control-surface atlases remain explanatory references | Can explain native alternatives, parameters, topology, state scope and risks; cannot inspect, insert, edit, automate, emit Accessibility actions, or operate those native objects | Four immutable Apple manuals, 2,686 pages total, were read in full; 16 canonical Quick Sampler pages close a PDF omission and a dated hash pins the mutable 12.3 release notes. Undocumented DSP and attached-device state still require direct tests | Generated catalogs contain 142 effects/tools, 28 instruments/utilities, and all 30 named editor tools; the effect catalog includes all 35 Pedalboard effects plus Mixer/Splitter. A fail-closed audit resolves every identity to primary-source pages. The 18-fixture/200-identity campaign ledger now records 189 `not_run`, 11 `partial`, and zero `complete`. Bitcrusher Default proved a bounded bypass/active/save-reload lane. Channel EQ proved settled default and bypass transparency plus deterministic 1000 Hz/+6 dB/Q1 behavior below the PCM24 clipping ceiling. Compressor proved the observed non-neutral Auto-Gain default, source-exact bypass, one Peak/hard-knee 4.1:1 static curve, settling, and save/reload. None closes remaining grids, models, timing, phase/stereo/rates, musical use, or listening. Manual adjectives remain heuristics and contradictions remain visible, never executable truth. **Comprehensive documentary knowledge; 11 bounded partial direct native-effect runs; no additional host authority; empirical campaign open.** |
| User-mediated production tutor (Guide Me) | Reads an open-ended production question, explicit source class, local capture analysis when relevant, user-reported chain, and bounded local preferences; writes a validated grounded answer, strategy options, exact manual procedures only when reviewed, and lesson/profile persistence | Can explain or guide a user through a reviewed Logic 12.3 procedure; cannot observe channel strips, inserts/settings, regions, automation, MIDI, project state, or whether a step was performed; has no host automation or AU graph mutation path. At `406b446` there is no typed Guide-to-Create handoff | Companion-only; bounded checksummed atomic 0600 lesson/profile stores; no network required for the complete offline path; confirmed outcomes are explicit and locally ranked, with per-item forget and delete-all controls; provider proposals remain untrusted and bounded | Tutor v1 remains 77/77 and T7 is **CLOSED BOUNDED, NOT PASSED** after a partial Logic exercise with no owner perceptual evidence. General Tutor passes 518/518 offline cases across 99 evaluated domains over 458 claims and 78 strategies; GP5 native open-question UI and GP6 local deletable personalization pass. GP0-GP7 pass; GP8/GP9 remain pending and owner-blocked. **User-mediated guidance only; no host observation/control authority and no broad-tutor usefulness claim.** |
| App Groups | Shared container plus supported IPC primitives | Shares product state and captured artifacts, not Logic project state | App and extension have the same Team-ID-prefixed `KDV9RC892F.com.marcboyer.logicaudioassistant` entitlement | The signed bundles resolve the shared container. The macOS-native Team ID form avoids the per-process App Data prompt that occurred with the earlier iOS-style `group.` identifier. Schema 1.1 uses atomic messages, heartbeat discovery, runtime epochs, monotonic command sequences, strict terminal correlation, expiry/hard-age limits, captured-instance binding, descriptor reads, SHA-256/WAV metadata checks, bounded `flock`, terminal-response reservation, 2,048-file/32 MiB message and 512-instance quotas, and fail-closed admission. [Apple App Groups](https://developer.apple.com/documentation/bundleresources/entitlements/com.apple.security.application-groups). **Production transport.** |
| XPC / process boundary | Typed local service calls when entitlement and lifecycle permit | Heavy work only; no new Logic authority | Sandboxed XPC requires correct group/Mach service design | Not implemented; App Group exchange is current fallback. **Candidate after signing spike.** |
| Core MIDI | Creates endpoints; sends/receives MIDI | Can drive user-mapped controls; no audio or project DOM | Native Apple Silicon; MIDI permission behavior to test | API documented, adapter not implemented. [Virtual endpoints](https://developer.apple.com/documentation/coremidi/midiendpointref). **Production for explicit mappings.** |
| Virtual MIDI endpoints | Software source/destination visible to Logic | Same as MIDI; feedback depends on Logic assignment/protocol | No private framework; user must connect/assign | Public API documented; direct Logic test pending. **Production candidate.** |
| Logic control surfaces / assignments | Bidirectional mapped mixer, transport and exposed plug-in parameters | Selected channel feedback and automation are possible for supported mappings; arbitrary project edits are not guaranteed | Apple Silicon supports built-in profiles/scripts; third-party legacy plug-ins are constrained | [Controller assignments](https://support.apple.com/en-ie/guide/logicpro/ctls71c31487/10.7/mac/11.0). **Capability-gated.** |
| Mackie Control / HUI | MIDI protocol for supported Logic surface profiles | Transport, bank/track selection, mixer and mapped plug-in control; semantics/profile state require tests | Uses built-in Logic support; user setup required | Logic guide documents control surfaces, not a general software-agent API. **Experimental until state feedback is verified.** |
| Logic key commands | Invokes user-configured commands | Can trigger transport/bounce/navigation; state readback absent | Requires user mapping/focus or MIDI assignment | Apple documents key-command mapping through control surfaces. **User-mediated only.** |
| Accessibility (`AXUIElement`) | Reads exposed semantic attributes/actions; can set or press exposed elements | Potential UI selection/insertion/bounce; reliability depends on Logic's accessibility tree | Accessibility permission required; sandbox entitlement/consent; UI/version sensitive | Apple API can return not-implemented, invalid, or cannot-complete errors. [AXUIElement](https://developer.apple.com/documentation/applicationservices/axuielement_h). **Feature-flagged experimental.** |
| Apple Events / AppleScript | Only the target app's published dictionary, plus UI scripting through System Events | Logic's installed `sdef` exposes generic application/document/window and print objects, but no track, region, mixer, insert, automation, transport, or bounce project model; UI scripting collapses to Accessibility | Automation and possibly Accessibility consent | `xcrun sdef /Applications/Logic Pro.app` inspected on Logic 11.2.2. **Not a stable project-control mechanism.** |
| Drag/drop or explicit import | User grants a file and places rendered alternatives | Full source file only when user supplies it; no selection inference | User-selected file permission; sandbox friendly | WAV reader/writer and offline tools work. **Production fallback.** |
| Bounce in Place | Logic renders track/region through its own UI command | Can create/replace material but is project destructive depending on options | User action/key command/AX; no public direct API found | Apple warns track replacement loses original content/most automation. [Bounce behavior](https://support.apple.com/en-mide/guide/logicpro/lgcp8aa3b784/10.7/mac/11.0). **User-mediated, never automatic in MVP.** |
| Buffer input in plug-in | Copies audio already passing through insert into bounded memory | Bounded recent interval; explicit “next” arming is not wired yet; not the selected source file | No microphone permission; no disk/file work on render | Atomic-payload ring and fail-closed chronology tests pass. The off-render bridge publishes an instance-bound recent-capture WAV with SHA-256 identity and matching rate/channel/frame metadata. Deallocation clears capture; direct/IPC reads fail and publish no WAV until new playback fills the empty reallocated ring. Logic 11.2.2 produced a verified 7.012-second 44.1 kHz stereo capture; Logic 12.3 produced a verified 661,500-frame/15-second 44.1 kHz mono capture. **Production core.** |
| ARA 2 | Host/plug-in share regions, audio files, structure and analysis | Directly addresses timeline/source access for compatible audio tracks | Logic supports ARA 2; third-party SDK/licensing and vendor approval required; operational limitations apply | [Logic ARA 2 support](https://support.apple.com/en-ie/guide/logicpro/lgcp58ce340b/10.7/mac/11.0). **Future licensed spike, not MVP dependency.** |
| User-mediated workflows | Explicit insert, capture, import, export, key command, drag/drop | Reliable because the user establishes scope and verifies result | Minimal permissions | **Primary fallback and production UX.** |
| Other official Logic extension system | AUv3, MIDI Device Scripts/control assignments, and ARA 2 were found | No official general project-editing extension was found | Varies | Recheck each supported Logic release. **No speculative authority.** |

## Direct answers to the spike questions

1. **Can an AU know the selected Logic region?** No public AU API found.
2. **Can it access the selected region's source file?** No through AUv3. User import works; ARA 2 may when licensed and correctly integrated.
3. **Can it access only audio passing through its insert?** Yes, through its input bus.
4. **Can it identify its host track?** `contextName` can contain host context such as a track label, but hosts choose what to provide; Logic behavior is unverified and must not be an identity key.
5. **Can it receive transport/timeline information?** AU host transport and musical-context callbacks exist; Logic delivery is pending host test.
6. **Can it buffer a bounded interval?** Yes. Incoming samples enter a preallocated atomic ring with no file I/O or explicit allocation on render, and an inconsistent snapshot fails closed. The ring is scoped to the current resource allocation; deallocation clears it and IPC cannot publish stale audio, while reallocation begins empty. Logic directly produced a verified recent-playback capture. “Capture next playback” remains open.
7. **Can a companion communicate with it?** Yes through the signed shared App Group: heartbeat discovery, instance-bound correlated capture, hashed/metadata-matched artifact resolution, capture-snapshot/expected-current-plan compare-and-swap commit, applied-heartbeat reconciliation, bypass, and runtime-epoch/expiry rejection are implemented and custom-host tested. Current-build Logic directly proved discovery, capture, preview/revision, commit, bypass/restore, reconnect, and two-instance routing.
8. **Can it manipulate its own parameters?** Yes, through `AUParameterTree`; host automation is part of the AU API. Output gain implements bounded sample-offset immediate/ramp events across callbacks; general graph-node event automation is not implemented.
9. **Can it control native Logic plug-ins?** No direct public project API. User MIDI mappings/control surfaces or Accessibility are limited adapters.
10. **Can it insert Logic/third-party plug-ins?** No stable public API found.
11. **Can it inspect an existing channel strip?** No stable public API found; partial control-surface feedback is not a complete strip model.
12. **Can it create automation?** Hosts can automate the product's exposed AU parameters. Arbitrary Logic automation is only a user-mapped/experimental control path.
13. **Can it create/duplicate tracks or regions?** No stable public API found.
14. **Can it safely invoke Bounce in Place?** No direct API. User invocation is safest; Accessibility/key-command automation is experimental and must verify every dialog/result.
15. **Can control-surface feedback determine selected track?** Possibly enough for a mapped surface, but identity and synchronization are unverified; not stable scope authority.
16. **Can Accessibility identify controls semantically?** Only to the degree Logic exposes roles, labels, values, actions, and stable hierarchy. Direct tests are required per version; coordinates are prohibited.
17. **What joins Logic native undo?** Host-recorded changes to exposed AU parameters and Logic's own commands may. Product graph snapshots and external UI sequences cannot assume native undo.
18. **What needs product snapshots?** Every graph proposal/commit, preview, node lock/removal, provider result, and all experimental project-action transactions.
19. **What remains impossible without new APIs?** A supported general Logic project DOM: arbitrary region/source access, channel-strip inspection, insert/reorder, track/region creation, and verified project transactions.
